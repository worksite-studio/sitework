{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd sitework && npm ci && npm run build",
  "outputDirectory": "sitework/dist",
  "installCommand": "echo 'install handled by buildCommand'",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
